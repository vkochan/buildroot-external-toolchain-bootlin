if BR2_TOOLCHAIN_EXTERNAL_BOOTLIN_AARCH64_UCLIBC

config BR2_TOOLCHAIN_EXTERNAL_PREFIX
	default "aarch64-buildroot-linux-uclibc"

config BR2_PACKAGE_PROVIDES_TOOLCHAIN_EXTERNAL
	default "toolchain-external-bootlin-aarch64-uclibc"

endif
